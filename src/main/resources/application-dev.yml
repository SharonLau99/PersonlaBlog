spring:
  thymeleaf:
    #开发者
    cache: false
  datasource:
    username: 
    password: 
    url: jdbc:mysql://host:3306/blog?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=FALSE
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    org.sharon: debug
  file:
    path: log/blog-dev.log
